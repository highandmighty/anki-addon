# Repository instructions

- Do not minify JavaScript files or update files in `docs/` unless the user explicitly asks for minification.
- The hardcoded SWC minification snippets for `parsers/parsers.js` and `pealim/pealim.js` are documented in `readme.md`.
- Use `git mv` for moving or renaming repository files; do not use plain `mv`.
